# Dependencies
/node_modules
/shruggbot-ui/node_modules

# Ignore .env files (but keep them available locally)
.env

# Build artifacts (only ignore if you're not deploying from them)
#/shruggbot-ui/build   <-- DON'T IGNORE if you need it deployed!

# Misc system files
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Old/deprecated directories (remove or uncomment as needed)
#/build
#/dummy text
